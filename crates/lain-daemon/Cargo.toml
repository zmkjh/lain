[package]
name = "lain-daemon"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
lain-core = { path = "../lain-core" }
lain-identity = { path = "../lain-identity" }
lain-dht = { path = "../lain-dht" }
lain-transport = { path = "../lain-transport" }
lain-nat = { path = "../lain-nat" }
lain-discovery = { path = "../lain-discovery" }
thiserror.workspace = true
tokio = { workspace = true, features = ["net", "sync", "time", "signal"] }
tracing.workspace = true
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
toml.workspace = true
tracing-subscriber.workspace = true
rand.workspace = true
